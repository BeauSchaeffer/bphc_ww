name: alignment
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Core alignment tools
  - bowtie2=2.5.*
  - samtools=1.20

  # Optional: lightweight utilities
  - python=3.10     # for scripting or small helpers
  - pip

  # (Optional) useful CLI tools if your pipeline uses them
  # - pigz           # for faster BAM/FASTQ compression
  # - bcftools       # variant manipulation if needed downstream

# conda env create -f env_alignment.yml